---
roles:
  # Install a role from Ansible Galaxy.
  #  - name: geerlingguy.java
  # version: "2.3.2" # обязательно фиксим версию, если моя роль зависит от этой роли. 
  - name: geerlingguy.jenkins
    version: "5.1.0"
  - name: ansible-ThoTeam.nexus3-oss
    version: "2.5.2"

collections:
  # Install a collection from Ansible Galaxy. На сайте пишут, что работает с ansible-core>=2.13.9. 
  - name: community.general
    version: ">=8.2.0"
    source: https://galaxy.ansible.com

